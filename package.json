{
  "name": "jsav-exercise-recorder",
  "version": "1.0.0",
  "description": "records steps from JSAV proficiency exercises and sends them to the server",
  "main": "exerciseRecorder.js",
  "scripts": {
    "build": "browserify exerciseRecorder.js > build/jsav-exercise-recorder-bundle.js",
    "test": "jest --watch --verbose"
  },
  "keywords": [
    "JSAV"
  ],
  "author": "Giacomo Mariani",
  "license": "ISC",
  "devDependencies": {
    "jest": "^28.1.0"
  },
  "dependencies": {
    "axios": "^0.21.1"
  }
}
